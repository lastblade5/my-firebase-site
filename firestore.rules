rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ⚠️ 개발용 임시 허용: 날짜 지나면 자동 차단
      allow read, write: if request.time < timestamp.date(2025, 8, 25);
    }
  }
}
